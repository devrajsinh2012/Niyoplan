---
phase: 08-google-drive-storage
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - app/api/files/route.js
  - app/api/files/upload/route.js
  - app/api/files/[fileId]/route.js
  - components/FileAttachment.jsx
  - __tests__/fileAttachmentRoutes.test.js
autonomous: true
requirements:
  - GDRIVE-03
must_haves:
  truths:
    - "Users can upload, list, download, and delete attachments within org/project/card boundaries."
    - "Uploads are rejected with clear errors when Drive is not connected or file exceeds 25MB."
    - "Users in one organization cannot access another organization's attachments."
  artifacts:
    - path: "app/api/files/upload/route.js"
      provides: "Multipart upload endpoint with Drive + metadata insert"
    - path: "app/api/files/route.js"
      provides: "Attachment listing endpoint scoped by org/project/card"
    - path: "app/api/files/[fileId]/route.js"
      provides: "Download URL + delete endpoint with membership checks"
    - path: "components/FileAttachment.jsx"
      provides: "Reusable upload/list/delete/download UI"
  key_links:
    - from: "components/FileAttachment.jsx"
      to: "/api/files/upload"
      via: "FormData upload via apiFetch"
      pattern: "apiFetch\('/api/files/upload'"
    - from: "app/api/files/[fileId]/route.js"
      to: "lib/drive.js"
      via: "getDriveFileDownloadUrl and deleteFileFromDrive"
      pattern: "getDriveFileDownloadUrl|deleteFileFromDrive"
---

<objective>
Implement end-to-end attachment lifecycle with Google Drive-backed storage and reusable UI.

Purpose: Deliver tenant-safe file operations for cards/projects without storing binary content in Supabase.
Output: Files API suite and FileAttachment component ready for embedding in product surfaces.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@google_drive_integrate.md
@lib/access.js
@lib/apiClient.js
@components/kanban/CardDetail.jsx
@database/schema.sql

<interfaces>
From database/schema.sql:
- organization_members(user_id, organization_id, role, status)
- cards(id, project_id)
- projects(id, organization_id)

From lib/access.js:
- verifyOrganizationAccess(orgId, userId)
- verifyProjectAccess(projectId, userId)

From lib/apiClient.js:
- apiFetch(url, options) with FormData passthrough
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build secure file attachment API routes</name>
  <files>app/api/files/route.js, app/api/files/upload/route.js, app/api/files/[fileId]/route.js, __tests__/fileAttachmentRoutes.test.js</files>
  <behavior>
    - Test 1: Upload larger than 25MB returns 413 with clear message.
    - Test 2: Accessing file metadata/url from another org returns 403.
    - Test 3: Successful upload inserts metadata only (no blob column writes).
  </behavior>
  <action>Create list/upload/download/delete routes using `getAuthUser` and membership/project validation patterns per GDRIVE-03. Use `card_id` (not `issue_id`) to match schema, enforce 25MB server-side guard, return human-readable errors, and call Drive helper methods for upload/delete/download URLs. Ensure delete allows uploader or org admin/pm role.</action>
  <verify>
    <automated>npx vitest run __tests__/fileAttachmentRoutes.test.js</automated>
  </verify>
  <done>All file routes enforce org boundaries, size limits, and Drive-connected preconditions with passing automated tests.</done>
</task>

<task type="auto">
  <name>Task 2: Create reusable FileAttachment client component</name>
  <files>components/FileAttachment.jsx</files>
  <action>Build `FileAttachment` with props `{ orgId, projectId, cardId }`, upload loading state, list rendering, file type icons, readable size formatting, and delete/download actions via `apiFetch` per GDRIVE-03. Show inline warning when uploads are unavailable because Drive is not connected. Keep component style compatible with existing card detail surfaces and avoid introducing new dependencies beyond `googleapis` added earlier.</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Component supports upload/list/download/delete lifecycle and exposes clear user feedback for success/error states.</done>
</task>

</tasks>

<verification>
- Run `npx vitest run __tests__/fileAttachmentRoutes.test.js`.
- Run `npm run lint`.
- Confirm route responses are org-scoped and errors are human-readable.
</verification>

<success_criteria>
- Attachments lifecycle works through API and reusable component.
- Size limits and disconnected-Drive scenarios are handled explicitly.
- Multi-tenant data boundaries are enforced for every file operation.
</success_criteria>

<output>
After completion, create `.planning/phases/08-google-drive-storage/08-03-SUMMARY.md`
</output>
