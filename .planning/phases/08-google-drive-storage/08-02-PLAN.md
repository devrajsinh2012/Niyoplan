---
phase: 08-google-drive-storage
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - app/api/drive/connect/route.js
  - app/api/drive/disconnect/route.js
  - app/api/drive/status/route.js
  - app/settings/company/page.jsx
  - __tests__/driveConnectionRoutes.test.js
autonomous: true
requirements:
  - GDRIVE-01
  - GDRIVE-04
must_haves:
  truths:
    - "Org admins can connect or disconnect Google Drive from Company Settings."
    - "Non-admin members cannot connect/disconnect Drive for the organization."
    - "Settings screen always shows current Drive connection state for active organization."
  artifacts:
    - path: "app/api/drive/connect/route.js"
      provides: "Admin-only org Drive connection and root folder bootstrap"
    - path: "app/api/drive/disconnect/route.js"
      provides: "Admin-only org Drive disconnection"
    - path: "app/api/drive/status/route.js"
      provides: "Org Drive status endpoint for settings UI"
    - path: "app/settings/company/page.jsx"
      provides: "Google Drive Storage settings panel"
  key_links:
    - from: "app/settings/company/page.jsx"
      to: "/api/drive/status"
      via: "apiFetch in effect on active organization change"
      pattern: "apiFetch\(`/api/drive/status"
    - from: "app/api/drive/connect/route.js"
      to: "lib/drive.js"
      via: "createDriveFolder orchestration"
      pattern: "createDriveFolder"
---

<objective>
Deliver organization-level Drive connection management and visibility in Company Settings.

Purpose: Enable admin-controlled onboarding/offboarding of Drive storage per tenant.
Output: Admin-protected Drive connect/disconnect/status APIs and a production-ready settings UI section.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@google_drive_integrate.md
@app/settings/company/page.jsx
@app/api/projects/route.js
@lib/auth.js
@lib/roles.js
@lib/apiClient.js

<interfaces>
From lib/auth.js:
- export const getAuthUser = async (request) => { user, error }

From lib/roles.js:
- export const checkRole = (user, ...allowedRoles)

From app/settings/company/page.jsx:
- activeOrganization.id from OrganizationContext
- apiFetch(url, options) for authenticated route calls
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement admin-gated Drive connection lifecycle routes</name>
  <files>app/api/drive/connect/route.js, app/api/drive/disconnect/route.js, app/api/drive/status/route.js, __tests__/driveConnectionRoutes.test.js</files>
  <behavior>
    - Test 1: Unauthenticated requests return 401 across all three routes.
    - Test 2: Non-admin org members receive 403 on connect/disconnect.
    - Test 3: Successful connect stores org connection and returns root folder metadata.
  </behavior>
  <action>Build routes using existing `getAuthUser` + `organization_members` access pattern (not direct Clerk `auth()` calls) per current codebase conventions and GDRIVE-04. `connect` should upsert tokens, create `Niyoplan/{OrgName}` folders via `lib/drive.js`, then persist `root_folder_id`. `disconnect` should delete the org connection row. `status` should return `{ connected, connectedAt, rootFolderId }` for the requested org after membership validation.</action>
  <verify>
    <automated>npx vitest run __tests__/driveConnectionRoutes.test.js</automated>
  </verify>
  <done>Drive lifecycle routes are protected, functional, and covered by route-level tests for auth and role boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Add Google Drive Storage panel to Company Settings</name>
  <files>app/settings/company/page.jsx</files>
  <action>Add a new settings card/section under Company Settings for Drive status and actions per GDRIVE-04. Use existing visual style, `apiFetch`, and `react-hot-toast`. On load and org switch, fetch `/api/drive/status?orgId=...`. If disconnected, show connect CTA and help text about admin-only action. If connected, show connected date + folder path and a disconnect button with confirmation. Keep controls hidden/disabled when user is not org admin.</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Company Settings displays accurate Drive status and provides admin-only connect/disconnect controls with user-friendly messages.</done>
</task>

</tasks>

<verification>
- Run `npx vitest run __tests__/driveConnectionRoutes.test.js`.
- Run `npm run lint`.
- Confirm `/settings/company` reflects Drive status for active organization.
</verification>

<success_criteria>
- Admins can connect/disconnect Drive for their org from UI.
- Non-admins are blocked from lifecycle endpoints and controls.
- UI state matches backend status endpoint data.
</success_criteria>

<output>
After completion, create `.planning/phases/08-google-drive-storage/08-02-SUMMARY.md`
</output>
