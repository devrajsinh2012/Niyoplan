---
phase: 08-google-drive-storage
plan: 04
type: execute
wave: 3
depends_on:
  - 08-02
  - 08-03
files_modified:
  - app/api/projects/route.js
  - components/kanban/CardDetail.jsx
  - __tests__/projectDriveFolder.test.js
autonomous: false
requirements:
  - GDRIVE-05
  - GDRIVE-06
must_haves:
  truths:
    - "Creating a project in a Drive-connected org auto-creates a Drive folder."
    - "Card detail can attach and manage files using the new reusable attachment UI."
    - "End-to-end flow is verified by human QA across connect/upload/download/delete scenarios."
  artifacts:
    - path: "app/api/projects/route.js"
      provides: "Post-create Drive folder bootstrap for new projects"
    - path: "components/kanban/CardDetail.jsx"
      provides: "Attachment feature entry point replacing placeholder attach action"
  key_links:
    - from: "app/api/projects/route.js"
      to: "lib/drive.js"
      via: "isOrgDriveConnected + createDriveFolder"
      pattern: "isOrgDriveConnected|createDriveFolder|getOrgRootFolderId"
    - from: "components/kanban/CardDetail.jsx"
      to: "components/FileAttachment.jsx"
      via: "prop-wired card/project/org context"
      pattern: "<FileAttachment"
---

<objective>
Wire Google Drive attachments into core product flows and complete release verification.

Purpose: Ensure storage provisioning and file actions happen naturally within project/card workflows.
Output: Project-create Drive folder automation, card attachment integration, and UAT sign-off checkpoint.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@google_drive_integrate.md
@app/api/projects/route.js
@components/kanban/CardDetail.jsx
@.planning/phases/08-google-drive-storage/08-02-PLAN.md
@.planning/phases/08-google-drive-storage/08-03-PLAN.md

<interfaces>
From app/api/projects/route.js:
- POST expects { name, description, prefix, organizationId }
- Permission gate: organization_members + checkRole(admin, pm)

From components/kanban/CardDetail.jsx:
- Prop contract: CardDetail({ card, onClose, onSave, onDelete, isSaving })
- Card object includes: id, project_id, title, ...
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Auto-provision project Drive folder on project creation</name>
  <files>app/api/projects/route.js, __tests__/projectDriveFolder.test.js</files>
  <behavior>
    - Test 1: When org Drive is connected, POST /api/projects creates a project folder in Drive.
    - Test 2: When org Drive is not connected, project creation still succeeds without Drive calls.
    - Test 3: Drive failures do not expose raw internal errors to users.
  </behavior>
  <action>Extend project creation flow to call `isOrgDriveConnected`, `getOrgRootFolderId`, and `createDriveFolder` after successful DB insert per GDRIVE-05. Persist `drive_folder_id` to project if migration/column exists; otherwise keep non-blocking behavior and log structured warning. Maintain existing project creation permissions and response shape.</action>
  <verify>
    <automated>npx vitest run __tests__/projectDriveFolder.test.js</automated>
  </verify>
  <done>Project creation automatically provisions Drive folder for connected orgs without breaking current create-project behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate FileAttachment into card detail workflow</name>
  <files>components/kanban/CardDetail.jsx</files>
  <action>Replace the current Attach placeholder toast action in card detail with embedded `FileAttachment` component wired with current `orgId`, `projectId`, and `cardId` context from loaded card/project state per GDRIVE-05. Keep existing modal/tab UX stable and avoid regressions in comments/subtasks editing.</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Users can upload/list/download/delete card attachments directly from Card Detail without placeholder behavior.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end Drive integration verification</name>
  <files>Manual verification checkpoint (no file edits)</files>
  <action>Execute the verification flow against the built system and capture pass/fail evidence for each step before phase sign-off.</action>
  <what-built>Drive connection UI + org-scoped APIs + file lifecycle + project folder auto-provisioning.</what-built>
  <how-to-verify>
    1. Open `/settings/company` as org admin and connect Google Drive.
    2. Create a new project in that org and confirm a Drive folder appears under `Niyoplan/{OrgName}/`.
    3. Open card detail, upload a file under 25MB, then download and delete it.
    4. Attempt same actions as a non-admin/non-member user and confirm forbidden behavior.
    5. Disconnect Drive and verify upload path shows clear disabled/warning message.
  </how-to-verify>
  <verify>
    <automated>npx vitest run __tests__/drive.test.js __tests__/driveConnectionRoutes.test.js __tests__/fileAttachmentRoutes.test.js __tests__/projectDriveFolder.test.js</automated>
  </verify>
  <done>All manual steps pass and automated suite is green; any failures are documented with reproduction details before resuming.</done>
  <resume-signal>Type "approved" to finalize phase, or provide failing step numbers with observed behavior.</resume-signal>
</task>

</tasks>

<verification>
- Run `npx vitest run __tests__/projectDriveFolder.test.js`.
- Run `npm run lint`.
- Complete checkpoint UAT flow and capture outcomes in SUMMARY.
</verification>

<success_criteria>
- New projects auto-create Drive folders when org storage is connected.
- Card-level attachment UX is fully functional and replaces placeholder action.
- Human verification confirms org isolation and lifecycle integrity.
</success_criteria>

<output>
After completion, create `.planning/phases/08-google-drive-storage/08-04-SUMMARY.md`
</output>
