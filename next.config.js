/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'standalone',
  env: {
    // Add any non-prefixed env vars here if needed
  },
}

module.exports = nextConfig
