---
phase: 08-google-drive-storage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - README.md
  - supabase/migrations/20260402_google_drive_integration.sql
  - lib/drive.js
  - __tests__/drive.test.js
autonomous: true
requirements:
  - GDRIVE-01
  - GDRIVE-02
  - GDRIVE-06
user_setup:
  - service: clerk-google-oauth
    why: "Google Drive API access requires delegated OAuth scope and refresh token from Clerk-managed Google login."
    env_vars:
      - name: GOOGLE_CLIENT_ID
        source: "Existing Google OAuth app used by Clerk"
      - name: GOOGLE_CLIENT_SECRET
        source: "Existing Google OAuth app used by Clerk"
    dashboard_config:
      - task: "Add OAuth scope https://www.googleapis.com/auth/drive.file"
        location: "Clerk Dashboard -> Social Connections -> Google"
      - task: "Enable refresh tokens"
        location: "Clerk Dashboard -> Social Connections -> Google"
must_haves:
  truths:
    - "Each organization can persist one Drive connection with refreshable tokens."
    - "Drive helper can create folders and upload/delete files for a specific organization without cross-org token leakage."
    - "Environment and dependency setup is explicit for local/dev/prod onboarding."
  artifacts:
    - path: "supabase/migrations/20260402_google_drive_integration.sql"
      provides: "org_google_drive and file_attachments schema with org-scoped linkage"
    - path: "lib/drive.js"
      provides: "OAuth client hydration, token refresh persistence, and Drive CRUD wrappers"
    - path: "README.md"
      provides: "Google Drive integration setup and required env vars"
  key_links:
    - from: "lib/drive.js"
      to: "org_google_drive"
      via: "supabaseAdmin reads and token updates"
      pattern: "from\('org_google_drive'\)"
    - from: "lib/drive.js"
      to: "google.drive v3"
      via: "googleapis OAuth2 auth client"
      pattern: "google\.drive\(\{ version: 'v3'"
---

<objective>
Establish Google Drive storage foundation for multi-tenant file handling in Niyoplan.

Purpose: Create the secure schema and Drive helper contracts everything else depends on.
Output: Migration, reusable Drive utility, dependency/docs updates, and baseline tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@google_drive_integrate.md
@database/schema.sql
@lib/supabaseServer.js

<interfaces>
From lib/supabaseServer.js:
- export const supabaseAdmin = createClient(...)
- export function createServerClient()

From database/schema.sql:
- public.organizations(id)
- public.projects(id, organization_id)
- public.cards(id, project_id)
- public.profiles(id)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add org-scoped Drive metadata schema and constraints</name>
  <files>supabase/migrations/20260402_google_drive_integration.sql</files>
  <action>Create migration with `org_google_drive` and `file_attachments` tables using existing table names (`organization_members`, `projects`, `cards`, `profiles`) and add RLS enable statements per GDRIVE-02. Include indexes for `org_id`, `project_id`, `card_id`, and `uploaded_at` to keep list/download paths fast at scale. Do not store file bytes in DB (metadata only, per GDRIVE-01).</action>
  <verify>
    <automated>node scripts/verify-migrations-simple.js</automated>
  </verify>
  <done>Migration compiles in Supabase validator script and schema expresses one Drive connection per org plus attachment metadata-only storage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build Drive API helper with token refresh persistence</name>
  <files>lib/drive.js, __tests__/drive.test.js</files>
  <behavior>
    - Test 1: Missing org connection throws a human-readable error.
    - Test 2: Refreshed access token from OAuth client persists to `org_google_drive`.
    - Test 3: `uploadFileToDrive` returns `driveFileId` and size metadata for DB insert.
  </behavior>
  <action>Implement `getDriveClient`, `createDriveFolder`, `uploadFileToDrive`, `getDriveFileDownloadUrl`, `deleteFileFromDrive`, `getOrgRootFolderId`, and `isOrgDriveConnected` in `lib/drive.js` using `googleapis` and `supabaseAdmin`. Add explicit comment describing required Clerk Google scope (`drive.file`) and refresh token requirement per GDRIVE-06. Keep helper org-scoped by requiring `orgId` in every public function.</action>
  <verify>
    <automated>npx vitest run __tests__/drive.test.js</automated>
  </verify>
  <done>Drive helper exposes all required operations and passing tests validate token refresh persistence and org isolation behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Install dependency and document environment setup</name>
  <files>package.json, package-lock.json, README.md</files>
  <action>Add `googleapis` package and document `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` plus Clerk dashboard scope+refresh-token setup in README per GDRIVE-06. Preserve existing auth flow guidance and avoid introducing extra cloud providers.</action>
  <verify>
    <automated>npm ls googleapis</automated>
  </verify>
  <done>Dependency is installed and README includes reproducible setup instructions for developers and deploy environments.</done>
</task>

</tasks>

<verification>
- Run `node scripts/verify-migrations-simple.js`.
- Run `npx vitest run __tests__/drive.test.js`.
- Confirm README includes Drive scope and environment variable documentation.
</verification>

<success_criteria>
- Database can store per-org Drive connection + attachment metadata without storing binary files.
- Shared Drive helper supports auth, upload, folder creation, and delete paths for later API routes.
- Setup documentation and dependency state are ready for downstream plans.
</success_criteria>

<output>
After completion, create `.planning/phases/08-google-drive-storage/08-01-SUMMARY.md`
</output>
