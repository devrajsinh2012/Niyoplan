# Phase 07: Tab Switch Refresh Fix - Wave 1 Plan

**Wave: 1**
**Objective**: Stabilize Global Authentication and Organization context to prevent redundant re-renders and full-page refreshes on window focus.

**Files to modify**:
- [AuthContext.jsx](file:///d:/Devrajsinh%20Gohil/DG_project/devraj%20project/Niyoplan/context/AuthContext.jsx)
- [OrganizationContext.jsx](file:///d:/Devrajsinh%20Gohil/DG_project/devraj%20project/Niyoplan/context/OrganizationContext.jsx)

## Tasks

<task>
<description>Update AuthContext to prevent redundant loading triggers on focus.</description>
<read_first>
- [AuthContext.jsx](file:///d:/Devrajsinh%20Gohil/DG_project/devraj%20project/Niyoplan/context/AuthContext.jsx)
- [lib/supabase.js](file:///d:/Devrajsinh%20Gohil/DG_project/devraj%20project/Niyoplan/lib/supabase.js)
</read_first>
<action>
- Modify `AuthContext.jsx` to only set `loading: true` if `user` is currently null/undefined during an `INITIAL_SESSION` or `SIGNED_IN` event in `onAuthStateChange`.
- Ensure initial session check at line 44 doesn't trigger unnecessary re-renders.
</action>
<acceptance_criteria>
- Switching tabs does not cause `loading` state to toggle to `true` if a user is already present.
- `grep "SIGNED_IN" context/AuthContext.jsx` still exists.
</acceptance_criteria>
</task>

<task>
<description>Optimize OrganizationContext for silent/background refreshes.</description>
<read_first>
- [OrganizationContext.jsx](file:///d:/Devrajsinh%20Gohil/DG_project/devraj%20project/Niyoplan/context/OrganizationContext.jsx)
</read_first>
<action>
- Update `refreshOrganizations` to skip `setLoading(true)` if `userOrganizations.length > 0` (treat as background update).
- This prevents pages from showing skeletons/loaders on focus.
</action>
<acceptance_criteria>
- `OrganizationContext.jsx` contains logic to skip `setLoading(true)` when data is already present.
</acceptance_criteria>
</task>

## Verification
- Test by switching away and back to the tab while logged in.
- Verify no full-page loader appears.
- Verify no skeleton screens appear on focus.
